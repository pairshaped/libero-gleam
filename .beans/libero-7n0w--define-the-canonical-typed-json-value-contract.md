---
# libero-7n0w
title: Define the canonical typed JSON value contract
status: completed
type: task
priority: high
tags:
    - json
    - transport
created_at: 2026-06-03T18:54:47Z
updated_at: 2026-06-03T21:26:54Z
parent: libero-lph9
---

> *This was generated by AI during triage.*

Define the single typed JSON representation that all generated Libero transport paths must encode and decode.

Scope:
- Document the shape for primitive values, custom types, constructor variants, lists, dicts or maps, tuples if supported, BitArray, Option, Result, and nested user types.
- Keep the existing conceptual model of type identity plus variant plus fields where it fits.
- Say exactly which Gleam shapes are unsupported and what diagnostic should be raised.

Acceptance criteria:
- One contract is referenced by generated push, RPC, SSR, page message, and client-context code.
- No transport path owns a private JSON dialect for user values.
- Contract hash and version are still part of the generated compatibility check.
- Snapshot coverage catches accidental shape drift.



Completion notes:

- `typed-json-v1` is now the named canonical JSON value contract.
- The generated JSON contract artifact includes `typed_value_contract`, so the contract hash covers the value contract name.
- `pages/protocol/json-wire-protocol.md` documents the canonical value shapes, unsupported shapes, and the rule that RPC, push, SSR, page message, and client-context values must use generated typed JSON codecs.
- `test/birdie_snapshots/canonical_typed_json_contract_artifact.accepted` pins representative endpoint, push, SSR, labelled, unlabelled, zero-field, BitArray, Dict, tuple, Option, Result, and nested user-type contract metadata.
- Follow-up implementation wiring remains tracked by the existing JSON transport Beans: `libero-3rwj`, `libero-s4ep`, `libero-pofq`, and `libero-r8s2`.
